# PinMe 部署与使用教程

本项目（**系统分析师移动书房**）是一个基于 Vite + Vue3 的静态前端项目。您可以使用 **PinMe** 命令行工具，将项目一键发布到去中心化网络（IPFS）上。

---

## 🚀 部署步骤

### 步骤一：安装 PinMe 命令行工具
请打开您的本地终端（Terminal），运行以下命令全局安装 PinMe：

```bash
npm install -g pinme
```

> **提示**：如果在 Mac 上安装遇到权限问题，可以尝试加上 `sudo`：
> ```bash
> sudo npm install -g pinme
> ```

---

### 步骤二：登录 PinMe 账号
在终端运行登录命令，这会自动打开您的默认浏览器，跳转到 PinMe 页面供您完成授权登录（连接钱包等）：

```bash
pinme login
```

*若因网络问题浏览器无法正常跳转，您也可以在 PinMe 网页控制台获取您的 `AppKey`，然后在终端手动绑定：*
```bash
pinme set-appkey <您的AppKey>
```

---

### 步骤三：本地项目打包
在部署之前，必须先将开发环境的 Vue 代码编译打包为浏览器可以直接运行的静态网页资源。
在项目根目录下运行：

```bash
npm run build
```

打包成功后，项目根目录下会生成一个 **`dist`** 文件夹，这就是我们要发布的目标文件夹。
*(注：项目已为您做好了 `base: './'` 相对路径优化和 Hash 路由优化，打包出的文件可直接完美适配 IPFS 和各种静态托管平台。)*

---

### 步骤四：一键上传部署
运行以下命令，将刚刚生成的 `dist` 目录上传到 PinMe 的去中心化网络中：

```bash
pinme upload dist
```

上传成功后，终端会打印出部署成果，包括：
* **IPFS CID**：该版本的唯一哈希值。
* **预览链接**：例如 `https://pinme.eth.limo/#/preview/Qm...` （**请复制完整的预览链接进行访问**）。

---

## 🌐 绑定自定义域名（可选）

如果您希望拥有一个固定且好记的域名，而不是每次上传都使用不同的哈希链接，可以在上传时直接绑定子域名：

```bash
# 绑定 systemanalyst.pinme.dev (根据您的 PinMe 账户额度)
pinme upload dist --domain systemanalyst
```

绑定成功后，即可通过固定的自定义域名进行访问。

---

## 🔄 日常开发更新流程

当您在本地修改了书本配置、阅读器样式或修复了 Bug 后，更新线上的流程如下：

1. 本地修改代码。
2. 运行打包命令：`npm run build`
3. 运行上传命令：`pinme upload dist`
4. 复制终端新输出的链接进行访问即可！

---

## ⚠️ 常见问题与注意事项

1. **不要上传 `src` 或 `node_modules`**
   PinMe 是静态托管工具，只能上传编译后的静态成果物（即 `dist` 文件夹）。请**千万不要**直接运行 `pinme upload .` 或 `pinme upload src`，否则会导致源码泄露并且无法在浏览器中正常运行。
2. **上传失败 / 提示 `Failed to fetch`**
   由于 PinMe 使用去中心化的 IPFS 网关（如 `eth.limo`），在国内直接访问或上传可能会因为网络波动而超时。
   * **解决办法**：请在上传时开启您的网络代理（VPN），并使用 **全局代理** 模式重新尝试。
3. **查看历史部署记录**
   如果您想查看之前上传的版本或清理历史版本，可以在终端运行：
   ```bash
   pinme list     # 列出最近上传的历史记录
   ```
